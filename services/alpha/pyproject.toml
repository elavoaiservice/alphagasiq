[project]
name = "alphagasiq-service-alpha"
version = "0.1.0"
description = "Alpha Intelligence Layer: AlphaSignal materiality engine and signal detection for AlphaGasIQ"
requires-python = ">=3.11"
dependencies = ["alphagasiq-schemas"]

[tool.setuptools.packages.find]
include = ["alpha_service*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
